# 数据库驱动：sqlite（默认，免安装）| postgres（需本机或远程 PostgreSQL）
DB_DRIVER=postgres

# 切换到 postgres 时使用以下连接信息
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rural_banquet
# 本机 PostgreSQL 一般无需 SSL；云数据库（Neon/Supabase 等）设为 true
PGSSL=false

JWT_SECRET=rural_banquet_secret_2026
PORT=3001
